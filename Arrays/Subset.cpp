/*
A subset of an array is a collection of elements from the array where the order of elements doesn't matter

A subsequence of an array is a set of elements that aren't necessarily adjacent in the array but that are in the same order as they appear in the arr
*/

#include <iostream>
using namespace std;
int main(int argc, char const *argv[])
{
    
    return 0;
}
